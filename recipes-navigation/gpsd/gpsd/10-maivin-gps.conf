# Switch the receiver's UART to 38400 8N1 before gpsd opens it, since gpsd's
# auto-baud probing misidentifies this u-blox module.  The device and rate must
# match DEVICES and GPSD_OPTIONS in /etc/default/gpsd.
[Service]
ExecStartPre=/usr/bin/gpscfg -p /dev/ttymxc3 -b 38400
